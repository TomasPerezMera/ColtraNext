// TODO: "Crear Producto"
// "Aquí puedes crear un nuevo producto para tu tienda."
// Luego, formulario [export function CreateForm()] con Input Fields para cada propiedad de product;
// <form onSubmit={handleSubmit}>
// <input type="text" name="product-name" value={data.name} 
// onChange={(e)=> handleChange({ e, setValues: setData, values: data })}
// </form>
// y así sucesivamente para cada campo. IMPORTANTE crear propiedad/campo SLUG!!
// que sea name="slug" value={data.slug} !!!
// debe contener Validaciones - ej "if (!data.name || !data.slug || !data.description){alert(xxx), return} !!!